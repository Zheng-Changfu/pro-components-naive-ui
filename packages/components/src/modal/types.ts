export interface ProModalDraggableOptions {
  /**
   * 拖拽时限制在屏幕边缘
   * @default true
   */
  sticky?: boolean
  /**
   * 下次打开弹窗时，是否重置拖拽的位置
   * @default false
   */
  // resetPositionOnNextOpen?: boolean
}
