import { cB, cE } from 'naive-ui'

export default cB('pro-form-item', [
  cE('label', `
    display: inline-flex;
    align-items: center;  
  `),
  cE('icon', `
    cursor: help;
     margin-inline-start: 4px;
  `),
])
