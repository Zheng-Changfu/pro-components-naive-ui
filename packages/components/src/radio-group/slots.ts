export interface ProRadioGroupSlots {
  // 扩展的
  'readonly': { value: any }
  'readonly-empty': { value: any }
}

export const proRadioGroupExtendSlotKeys = [
  'readonly',
  'readonly-empty',
] as const
